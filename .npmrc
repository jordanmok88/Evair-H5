# Leave optional dependencies enabled. Vite/Rollup install a small native binary per platform
# (e.g. darwin-arm64 locally, linux-x64 on Cloudflare). `omit=optional` breaks that and can
# make `npm ci` fail on mismatched lockfiles.
