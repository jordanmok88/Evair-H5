Date range covered: 2026-05-16 02:02 UTC to 2026-05-17 02:02 UTC

- No PRs were merged to `main` in this window (`gh pr list` returned none; `origin/main` had no commits in the range).
- Draft PR #9, "Fix critical checkout, auth retry, and top-up routing bugs," was opened against `main` with four commits on `cursor/critical-correctness-bugs-6d72` (8657406, 4c11a44, 45c6b49, bcdf071).
- Payment integrity / security-relevant change in PR #9: `netlify/functions/stripe-checkout.mjs` now rejects legacy browser-priced payloads, requires `orderNo` plus `Authorization`, and proxies checkout creation to Laravel `/app/payments/checkout` so pricing comes from the server order (8657406). CORS was expanded to allow the `Authorization` header, and a new `stripe-checkout.test.ts` covers rejection of browser-priced payloads and safe return URL proxying.
- Auth stability change in PR #9: `services/api/client.ts` now caps a logical request at one 401 refresh retry, clears tokens, and fails closed if the retried request is still unauthorized; a regression test covers the no-infinite-refresh behavior (4c11a44).
- Top-up / wallet correctness change in PR #9: `/top-up` now carries through the supplier type that actually produced the recharge package, including PCCW-to-eSIMAccess fallback, and `App.tsx` mirrors SIM updates into `serverSims` plus refreshes user SIMs after logged-in purchase success (45c6b49).
- Dependency/security inventory: no package/dependency file changes were observed in the 24-hour window; the main security-sensitive work was the checkout proxy hardening above.

Watchlist:
- PR #9 is still open and marked draft, so none of the fixes above are merged to `main` yet.
- PR #9 checks are failing: Cloudflare Workers build, Netlify deploy-preview, and Netlify header/pages/redirect checks all reported `FAILURE` around 2026-05-16 02:11 UTC.
- Because PR #9 touches payment checkout, auth retry, and supplier routing, it needs an end-to-end smoke pass before merge despite the PR body listing `npm test`, targeted Vitest tests, and `npm run build` as validation.
